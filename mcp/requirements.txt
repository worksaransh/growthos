# GrowthOS MCP Server dependencies
# Install with: pip install -r mcp/requirements.txt

mcp[cli]>=1.0.0          # FastMCP framework (includes mcp.server.fastmcp)
httpx>=0.27.0            # Async HTTP client for Meta, Google, Shopify APIs
python-dotenv>=1.0.0     # .env loading (optional — server.py loads manually too)
